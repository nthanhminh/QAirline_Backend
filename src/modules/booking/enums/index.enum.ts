export enum EBookingStatus {
    "ACTIVE" = "ACTIVE",
    "CANCELLED" = "CANCELLED"
}

export enum ECustomerType {
    ADULT = "ADULT",
    CHILD = "CHILD"
}

export enum ECheckType {
    UPDATE = "UPDATE",
    CANCELLED = "CANCELLED"
}